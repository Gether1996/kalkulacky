# Shell scripts must stay LF so they run inside Linux containers even when the
# repo is checked out on Windows (autocrlf would otherwise break the shebang).
*.sh text eol=lf
entrypoint.prod.sh text eol=lf
